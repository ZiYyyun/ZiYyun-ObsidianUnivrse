#理论/CS 


### 指针的定义

### 指针的声明
**典型错误纠正**：  
`int *p = 89;` 中 `p` 存储的是**地址89**（十进制），而非让 `*p` 的值为89。  
正确做法：`int x=89; int *p = &x;`（此时 `*p` 才是89）。

### 指针的符号
`*(&变量) ≡ 变量` 且 `&(*指针) ≡ 指针`（二者互为逆运算）。

| 符号   | 含义             | 关键特性                |
| ---- | -------------- | ------------------- |
| `a`  | 变量本身的值         | 普通变量：存储数据；指针变量：存储地址 |
| `*a` | `a`指向的地址里的数据   | 仅当`a`是指针时有效！        |
| `&a` | `a`这个变量在内存中的位置 | 永远返回地址（十六进制形式）      |

