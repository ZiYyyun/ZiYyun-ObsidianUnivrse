#IC 

> [!NOTE] 总结
> CST816D 芯片会自己检测电容变化，并计算出触摸坐标，把 X、Y 分别存到内部寄存器中。当用户触摸屏幕时，芯片通过 INT 引脚通知 STM32。STM32 的中断函数不读取数据，只把 `is_touch` 置为 `true`。主程序发现有触摸事件后，再通过 I2C 从 `0x03` 开始连续读取 4 个寄存器，其中 X、Y 各占 12 位，高 4 位和低 8 位分别存放，需要通过移位和按位或重新拼接成完整坐标。最后根据屏幕安装方向，对 X 坐标做一次镜像，使触摸坐标和 LCD 显示坐标一致。










